// SLICE 2: Replace with Sidebar + TopNav layout.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen bg-bg-base text-text-primary">{children}</div>;
}
